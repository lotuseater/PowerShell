---
name: compact-test
description: Use when running this repository's build or tests. Bounds output via Invoke-Bounded.
---

# compact-test (Codex)

`Invoke-RepoTest -TestPath <narrow>` and `Get-WizardLog -LogPath ... -Range tail:200` for failure investigation.
